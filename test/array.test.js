const assert = require('assert');

describe('Test', () => {
    it('running console log', () => {
        
        // leaving this console log in for now will take out once all methods have passed.
        console.log('mocha is working');
    });
});