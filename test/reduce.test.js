/* eslint-disable */
const assert = require('assert');
const reduce = require('../lib/reduce');

it.skip('reduce', () => {
    const origArr = [1, 2, 3];
    const test = reduce(origArr, callback, initValue);

    assert.Equal = (accumulator, 6);
});